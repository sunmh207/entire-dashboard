优化CheckpointDetailSlideover.vue左侧Files展示效果
1.目前样式：每个文件一行
2.期望样式：将文件以树形结构展示(参考截图）。

给出设计方案

---

不需要提供 列表/树形两种选择，只提供树形结构展示。 也不需要提供搜索功能。 按照最小可行实现。

---

再优化，如果目录是连续的（中间都没有文件），则显示在同一行，不需要层级显示。只有有文件的目录才需要层级显示。